
const fs = require('fs');
const path = require('path');
// Since Node natively doesn't have shift_jis buffer decoding, we might need to rely on child_process to invoke powershell.
// OR we can just use the replace script!

